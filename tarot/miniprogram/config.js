// ============================================================
// 全局配置 —— 部署后按注释逐项填写（详见 ../README_部署.md）
// ============================================================
module.exports = {
  // ① 云开发环境 ID：云开发控制台 → 概览/设置 → 环境 ID（形如 fatetell-xxxxx）
  cloudEnv: '请替换为你的云开发环境ID',

  // ② 客服 / 私域微信号：在「解读结果」「我的」页作为私域钩子展示，用户可长按复制
  serviceWechat: '请替换为你的客服微信号',

  // ③ 是否走「模拟支付」：
  //    true  = 开发者工具里免商户号直接到账，用来跑通付费闭环（联调期用）
  //    false = 走真实微信云支付（需开通云支付 + 商户号）
  //    ⚠️ 上线务必改 false；且云函数 pay 要配 ALLOW_MOCK_PAY=1 模拟支付才真生效
  mockPay: true,

  // ④ 价格展示（仅前端文案；真实计价以云函数为准，云函数环境变量可覆盖）
  pricing: {
    readingCredits: 290,   // 深度解读消耗积分（对应云函数 PRICE_READING）
    signupGrant: 300       // 新人注册赠送（对应云函数 SIGNUP_GRANT_CREDITS）
  }
};
